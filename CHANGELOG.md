# Listener 发布公告

## 当前公告

### 1.0.1 · 2026-08-16

- 公共静态版启动不再向 Apple、Audius、MusicBrainz、Wikimedia Commons 和 Internet Archive 发起伪搜索健康检查，首屏第三方请求由 5 个降为 0，消除 MusicBrainz 偶发 `503` 在浏览器控制台产生的无意义资源错误。
- 来源状态改为准确表达“已配置 / 已接入”，实际搜索仍并行请求 5 个公共来源并逐一隔离超时、限流和服务故障；单一平台不可用不会隐藏其他平台结果。
- 删除已失去用途的状态探测超时参数，补齐零启动请求、立即返回、取消信号与 MusicBrainz 请求间隔回归测试。
- 版本、PWA 缓存、User-Agent、设置页与公告统一升级为 1.0.1。

## 历史公告

### 1.0.0 · 2026-08-16

- 新增 Internet Archive 官方 Advanced Search 与 Metadata API 搜索，Node 聚合和 GitHub Pages 静态版都能把公开音频条目展开为真实可播放的 MP3、Ogg Vorbis 与 FLAC 文件，免 Key 公共来源扩展到 5 个。
- Archive 播放地址只由经过校验的条目标识与文件名构造，过滤路径穿越、伪格式、超大文件、受限条目和异常响应；只有明确 Creative Commons 或 Public Domain 许可的歌曲显示下载，未知授权保持可播但不可下载。
- 地址 / ID 解析扩展到 19 个平台，新增 Internet Archive 条目页识别；搜索、详情、播放、下载、分页、取消、超时、响应大小、重定向与固定 Host 边界均有回归测试。
- 版本、PWA 缓存、User-Agent、设置页、来源状态、部署说明、许可证公告与发行公告统一升级为 1.0.0。

### 0.10.4 · 2026-08-16

- 搜索相关度升级为分层排序：完全匹配保持最高优先级，同等标题/歌手匹配内再按完整播放、试听和元数据排序；修复不可播同名元数据占满首屏，同时阻止无关完整音频越级。
- 移动端导航、搜索框、快捷词、来源与播放筛选、高级筛选、地址解析及探索入口统一达到至少 44px 触控目标，并同步修复仍要求旧 36px 高度的回归测试。
- 完成首页真实曲目、Taylor Swift 多源搜索、Audius 完整播放、收藏、账号页、封面样式、390px/320px 布局、单一音轨、控制台和资源请求复查；未发现横向溢出、控制台异常或 404。
- 保持歌曲为唯一媒体输出，不添加会打断音乐的点击 SFX、隐藏 BGM、第二条音轨或 AudioContext；生产构建、全量测试和依赖漏洞检查同步通过。

### 0.10.3 · 2026-08-11

- 跨来源相关度排序现在优先显示标题、歌手与专辑的真实直接匹配，避免无关的长篇开放录音挤到热门歌手搜索前列；仍保持完整直连优先和来源公平，不生成或伪造歌曲。
- 媒体地址失效后会从自动播放队列彻底移除该曲目，避免列表循环后再次解析同一坏音源；搜索、首页、收藏、歌单、推荐和最近播放继续同步标记失效状态。
- API 入口兼容健康、账号、地区与聚合搜索等路径的尾斜杠变体并直接响应，消除缓存路径差异带来的 404；设置页公告链接改为稳定 CHANGELOG 地址，不依赖尚未创建的版本 Release。
- 移动端菜单、头像、歌曲操作、播放器、歌单操作、外观选项和强调色色板均达到至少 44px 触控目标；复查 320px/390px 布局无横向溢出。
- 完成真实 Taylor Swift 聚合排序与 Audius 播放、深夜主题、收藏、设置、单一音轨、控制台、404、构建、测试和依赖漏洞检查；继续不添加干扰歌曲的 SFX、隐藏 BGM、第二条音轨或 AudioContext。

### 0.10.2 · 2026-08-10

- 搜索结果可继续加载最多 10 页、500 首，并按来源与歌曲 ID 跨页去重；“仅完整可播”现在只保留已有真实直连的完整音频，完整直连、试听、待解析与元数据统计不再混用。
- Audius 公开歌曲直接使用官方 Stream Gateway，由浏览器原生音频元素处理跨域播放；移除会被存储节点 CORS 误判的媒体字节探测，完整音源优先于 Apple 试听。
- 音源卡住或真实媒体错误后会在 5 秒内失效并自动前进到队列中的下一首完整直连歌曲；失效状态同步到搜索、首页、队列、收藏、歌单、相似推荐与最近播放，避免再次命中坏 URL。
- 新关键词会清除旧来源筛选；分页失败显示明确重试操作，达到 500 首安全上限时不再误报来源末页。桌面和移动端均提供对应的加载、末页、错误与完整歌曲提示。
- 播放器继续保持单一音乐音轨，不加入会打断歌曲的点击音效或隐藏 BGM；补齐分页取消、坏节点切换、严格播放筛选、移动触控与版本公告回归覆盖。

### 0.10.1 · 2026-08-09

- Audius 完整音频在播放前最多通过 3 次官方 Stream Gateway 请求验证真实媒体字节，只接受 HTTPS、`200/206` 与音频 MIME；失效节点不再被当作可播放歌曲。
- 音源验证失败后会同步降级当前歌曲、搜索、首页、队列、收藏、歌单与相似推荐，移出自动播放队列并保留官方来源跳转，避免反复解析和无限重连。
- 聚合来源默认等待上限由 8 秒缩短为 4 秒，浏览器公共多源搜索也加入同等预算；慢来源不再长时间遮住 Apple、Audius 等健康来源的结果。
- 搜索探索区在移动端默认折叠，高级筛选保持可读双列并压缩空白，让结果标题进入播放器上方首屏；菜单、队列、歌单与歌词遮罩使用互不重复的无障碍名称，并补齐媒体探测、超时降级、移动布局和版本一致性回归测试。

### 0.10.0 · 2026-08-09

- 搜索默认升级为“可直接播放优先”：开放完整音频优先，其次为官方试听，再显示需要二次解析的完整候选与仅元数据；主搜索容量提升到 50 首，前半页保证播放成功率，后半页按来源公平补齐，避免 Apple 或 Audius 单源吞满结果。
- Audius 只把同时具备官方 `access.stream`、可用、可串流且无门槛的歌曲列为完整候选；播放不再固化网关随机返回的失效节点，而是每次通过官方 Stream Gateway 重新选择节点且不泄露服务端 Key。
- 搜索行新增“完整可播 / 试听可播 / 完整音源·点击验证 / 站内不可播”真实状态，统计改为“直接可播 / 完整候选 / 总数”；仅元数据的左侧按钮会打开已验证的官方来源页，并取消仍在后台解析的旧歌曲，避免意外串播。
- 修复新增排序函数遗漏 TypeScript 声明导致生产构建失败；完成全量单元、生产构建、依赖漏洞、真实周杰伦/Taylor Swift 聚合、媒体字节、来源跳转、桌面/390px Chromium、控制台与 404 检查。

### 0.9.0 · 2026-08-08

- GitHub Pages 不再只有 Apple：新增浏览器公共多源聚合，静态版可并行搜索 Apple Music、Audius、MusicBrainz 与 Wikimedia Commons，任一来源故障不会隐藏其他结果。
- Audius 官方公开只读接口无需 Key 即可搜索；明确开放的歌曲支持完整播放，只有同时声明下载授权的歌曲才显示下载。Node 聚合端也默认启用 Audius，开发者 Key 改为可选。
- Pages 默认隐藏试听并优先呈现完整音频与元数据；18 个平台的地址 / ID 均可在浏览器本地识别。搜索标题、来源状态、降级提示、设置能力说明和部署文档已同步修正。
- 公共请求增加响应大小、HTTPS、Host、超时和取消边界，并严格遵守 MusicBrainz 每秒最多一次请求；慢来源不再阻塞整个状态检查，音频连续缓冲 15 秒会自动停止并给出可操作提示。
- 版本缓存和项目公告更新至 0.9.0。

### 0.8.1 · 2026-08-08

- 新增生产级 Node API 容器：镜像只安装生产依赖并复制服务端代码，以非 root 用户运行，SQLite 持久化到 `/data`，内置 `/api/health` 容器健康检查。
- 新增全来源 Compose 部署，启动前强制要求 Brave、YouTube、Audius 三项服务端 Key，默认允许 GitHub Pages Origin，避免密钥缺失后误以为已经上线全部平台。
- 新增部署验收命令，严格检查 HTTPS、CORS、健康响应以及 18 个平台的搜索能力；Apple-only 或任一来源缺失都会直接失败。

### 0.8.0 · 2026-08-07

- 新增 Brave 官方 Web Search 公开网页目录 Provider：QQ、酷狗、酷我、千千、一听、咪咕、荔枝、蜻蜓、喜马拉雅、5sing 原创/翻唱和全民 K 歌现在可按名称检索已索引曲目页；每条结果必须再次通过官方域名及地址/ID白名单，不调用逆向私有接口。
- 聚合服务支持一个受控适配器发布多个平台来源，保留来源级能力、状态、缓存、超时和错误隔离；公开网页目录只返回元数据和来源页，不伪造播放、歌词或下载权限。
- 搜索页新增“搜索平台”选择，可在请求前选择全部已接入平台或指定单一平台；修复单平台请求失败会被显示成普通零结果的问题。
- Pages 工作流不再强制 Apple-only，配置 `MUSIC_API_BASE_URL` 后连接 Node 聚合服务，未配置时仍安全回退 Apple 且不请求不存在的 `/api`。

### 0.7.0 · 2026-08-06

- 新增 YouTube Music 官方 Data API v3 Provider：支持名称搜索、公开视频详情、ISO 8601 时长、HTML 实体和官方缩略图；API Key 仅留服务端，搜索固定一页并受缓存保护，主页推荐不调用 YouTube 且离开主页会取消后台请求。
- 地址 / ID 解析扩展为 18 个平台，新增 YouTube Music、YouTube watch/shorts/embed、`youtu.be` 与 11 位视频 ID，并完善 QQ、网易、酷我、咪咕、全民 K 歌移动分享地址、无协议地址和单链接分享文案识别。
- 设置中心现在完整列出“已接入 / 仅解析”与搜索、播放、歌词、下载边界；YouTube 结果明确为仅元数据，不抽取音视频，也不绕过广告、登录、会员、地区或 DRM。
- YouTube 官方封面可通过受限 Host 下载；新增来源徽标样式，保持单一音乐音轨，不添加点击 SFX、第二条 BGM 或 AudioContext。

### 0.6.0 · 2026-08-05

- “喜欢的音乐”升级为完整虚拟歌单，支持顺序播放、随机播放和单曲循环；底部随机键改为持久模式，自动下一首与手动下一首遵循当前模式。
- 新增设置中心，集中管理系统/暖纸/深夜主题、强调色、界面字号、圆角、背景纹理、列表密度、减少动效和桌面播放器布局；歌曲封面新增黑胶、磁带与纯封面三种样式，悬浮播放器在手机端自动回退为安全贴底布局。
- 聚合搜索新增歌曲名/歌手/专辑字段筛选、三档时长范围和相关度/歌曲名/歌手/时长排序，并加入流派、场景、年代、地区四组真实搜索入口；筛选只整理 Provider 返回的数据，不生成或伪造歌曲。
- 设置页按当前部署真实状态展示音乐源的搜索、播放、歌词和下载能力，并区分“已连接”与“仅地址解析”；新增 GitHub 仓库、Issue、Commit、Release 和 HZAGAMING 主页入口。
- 新播放与外观偏好纳入 JSON 迁移和账号云同步；修复服务端旧状态白名单会静默丢弃新增偏好、非法样式值未被拒绝的问题，并继续拒绝把未授权平台伪装为可播放或可下载来源。

### 0.5.0 · 2026-08-05

- 新增 SQLite 账号系统、scrypt 密码哈希、HttpOnly 会话、修订号云同步、播放记录，以及不含凭据和临时音源的 JSON 导入导出；跨设备同 ID 歌单会合并曲目，首次登录恢复云端当前歌曲与偏好，保存期间的新修改不会漏同步。
- 所有歌曲行和播放器均提供封面下载；Wikimedia Commons 明确开放的完整音频通过固定 Host、MIME、重定向和 128 MiB 上限校验后以附件流式下载，不落盘、不缓存，Apple/网易等未授权来源仍明确拒绝音频下载。
- 新增 PWA Manifest 与离线应用壳；Service Worker 不缓存账号 API、第三方音频或私人数据。地区推荐只接受可信国家代码 Header，不保存原始 IP，并支持浏览器语言回退、手动选择和关闭。
- 首页扩展为周杰伦、Taylor Swift、The Weeknd、Dua Lipa、Bruno Mars 与地区流行搜索，分批请求并优先展示完整音源；未登录状态接口返回 200，生产子路径、Manifest、Service Worker 和深链接继续避免 404。

### 0.4.23 · 2026-08-05

- 自建歌单打开后会基于主要歌手或专辑展示最多 8 首真实相似结果；支持继续显示、Provider 分页和队尾预取，连续播放只追加明确授权的完整音源，没有新完整音源时循环已有可播放队列而不伪造歌曲。
- 首页改为实时展示周杰伦与 Taylor Swift 的真实目录歌曲；推荐结果排除试听、演示源和 Fixture，跨来源按歌曲身份去重，推荐状态不写入本地存储。
- 修复 iTunes Search 忽略 `offset` 导致 Apple 与聚合搜索跨页重复、主页重试误清理整个应用，以及无完整推荐时连续扫满 100 页的请求瀑布；切换歌单或卸载页面时会取消过期请求。
- 全量测试、生产构建、真实两页聚合 API、桌面与 320px Chrome、唯一音轨、控制台、4xx/5xx、静态资源和依赖漏洞检查均通过；继续不添加点击 SFX、第二条 BGM 或 AudioContext。

### 0.4.22 · 2026-08-04

- 网易云实验性公开目录搜索改为默认启用，补齐安全分页、数字 ID 详情查询和一次批量详情补全；搜索摘要缺少封面时会补齐可信 `music.126.net` 封面，补全失败仍保留可用结果。
- Node 聚合模式默认隐藏 Apple 试听，保留完整可播曲目与仅元数据目录，并提供“仅完整可播 / 包含试听”显式切换；四个默认来源现在并发请求，避免新增来源排队。
- 网易云结果只提供真实公开元数据，不伪造签名、不解密、不构造或伪装完整播放地址；全量测试、生产构建、真实 API、桌面/320px Chromium 和依赖漏洞检查均通过。

### 0.4.21 · 2026-08-04

- 新增默认启用的 Wikimedia Commons 官方公开音频源，通过 MediaWiki Action API 搜索可直接播放的开放音频；支持文件页 ID/地址解析、详情查询和完整音频播放。
- 对 API、媒体和来源页分别执行 HTTPS 与精确 Host 白名单校验，只接受真实音频 MIME，清理跟踪参数，并继续关闭未经明确授权的下载能力。
- 修复 MusicBrainz 大分页偏移错误、Provider User-Agent 版本过期，以及平台 ID 下拉误显示内部 Fixture 测试源的问题；三来源状态文案在窄侧栏中可安全换行。
- 搜索结果继续按来源交错，单个来源超时或返回异常不会阻断其他来源；保持唯一 `<audio>` 音轨，不添加会干扰音乐的点击 SFX 或第二条 BGM。
- 安全审计将 PostCSS 间接依赖升级到已修复版本；全量测试、三种生产构建、桌面/320px Chromium、媒体播放、控制台、404 与依赖漏洞检查均通过。

### 0.4.20 · 2026-08-03

- 修复音乐聚合内核只存在于代码、默认 `npm run dev` 却仅启动前端的问题；现在一条命令同时启动 Node API 与 Vite，并通过同源 `/api` 使用真实聚合搜索。
- MusicBrainz 官方 CC0 元数据源默认启用，与 Apple 搜索稳定交错返回；请求使用项目联系标识、官方 Host 白名单、每秒一次调度、8 秒超时、2 MB 响应上限和一次有限重试。
- GitHub Pages 继续使用已验证的 Apple 公共检索，不探测无法运行的 Node API；需要纯前端调试时可显式运行 `VITE_PUBLIC_APPLE=true npm run dev:client`。
- 真实 API 与 Chromium 验证“周杰伦”返回 20 首，页面同时显示 Apple Music、MusicBrainz，健康和搜索接口均为 200，控制台无错误。

### 0.4.19 · 2026-08-03

- 播放期间遇到浏览器网络切换或临时断流时，播放器会等待 800 ms 后自动重新装载并恢复播放；每次播放尝试最多自动重试一次，避免无限重连。
- 用户暂停、清空队列、移除当前歌曲、系统媒体暂停或切歌时会取消待执行的重连；切歌还会主动终止旧媒体请求，减少前后两首音频并行残留。
- 演示与本地音频持续失败时给出明确提示；远程临时音源持续失败时仍会失效并等待用户重新解析，不把解码错误误判为网络故障。
- 补齐媒体错误决策与切歌清理回归测试，并以真实 Chromium 注入 `MEDIA_ERR_NETWORK`，验证只重载一次、成功恢复、切歌后保持唯一音轨。

### 0.4.18 · 2026-08-02

- 公共 Apple 响应改为边读取边执行 2 MB 上限；即使上游缺少 `Content-Length`，超限时也会立即取消流，不再先把完整异常响应载入内存。
- 公共适配器的重试策略与后端安全 Client 对齐：只重试网络失败、429、502、503、504，并在唯一一次重试前执行可取消的短暂抖动退避；普通 500/501 和 4xx 不重试。
- 更新音乐内核集成审计，准确记录已经实现的安全 HTTP Client、Fixture、LRC、分页 API、缓存、限流和结构化日志，不再把已完成模块列为缺口。
- 真实 Chromium 再次验证“周杰伦”20 首结果与“晴天”试听，状态、搜索、音频响应为 200/200/206；桌面和 320px 页面无 4xx/5xx、控制台错误或横向溢出。

### 0.4.17 · 2026-08-02

- 修复只启动前端时仍探测未运行的 Node API、搜索先报资源错误并落入演示库的问题；未显式配置 `VITE_MUSIC_API_BASE` 时现在直接使用 Apple 公共搜索。
- Apple 公共请求对网络异常、429 和 5xx 自动重试一次；CN 店面没有匹配时补查 US 店面，减少瞬时链路故障和区域差异造成的空结果。
- Node 聚合服务离线时，搜索、Apple 地址/ID 详情及音源解析会回退到公共 Apple；页面区分公共结果与演示结果，并提供显式重试按钮。
- 真实 Chromium 验证“周杰伦”返回 20 首且首条为“晴天”；生产页面、Apple 请求、控制台和资源加载均无 4xx/5xx，保持唯一音乐音轨。

### 0.4.16 · 2026-08-02

- Pages 公共音乐源现在通过有界 GET 实际检查可用性，不再用 Chromium 会标记为 `ERR_ABORTED` 的 HEAD 探针，也不再离线时错误显示在线。
- 新增 Apple Music 地址与纯 ID 识别、详情查询；公共响应限制为 2 MB，校验国家代码，并继续分别限制音频、封面和来源 Host。
- 将误导性的 `VITE_STATIC_DEMO` 删除并改为 `VITE_PUBLIC_APPLE`；Pages 只展示实际支持的 Apple 解析选项，并使用对应的搜索标题、提示和状态文案。
- 修复 320px 搜索标题拆成单字换行及播放器“正在播放”小字对比度不足；保持唯一音乐音轨，不添加 SFX、第二条 BGM 或 AudioContext。
- 真实 Chromium 覆盖桌面/移动搜索、试听、URL 解析、断网降级、队列、歌单弹窗和移动菜单；全部无 4xx/5xx、控制台错误、横向溢出或 axe 违规。

### 0.4.15 · 2026-08-01

- 修复 GitHub Pages 仅搜索 8 首内置演示曲、普通歌名始终没有结果的问题；静态站现在直接连接无需密钥且支持 CORS 的 Apple Music 公共检索。
- 公共适配器支持真实歌曲元数据、封面与授权试听，对音频、封面和来源 URL 分别执行 Host 白名单校验；网络异常时明确降级到演示曲库。
- 真实 Chromium 搜索“周杰伦”返回 20 首结果并成功播放“晴天”试听；请求状态 200，页面、控制台与资源加载无错误。
- 补充公共搜索正规化、恶意媒体字段过滤及离线降级回归测试；本地 Node 聚合搜索保持不变。

### 0.4.14 · 2026-07-31

- 修复移动菜单打开后焦点仍滞留在已禁用主内容的问题；菜单现在立即聚焦内部控件，Tab 与 Escape 的焦点闭环保持完整。
- 队列与移动菜单改用合法的 dialog 容器语义，并提升章节编号、列表序号、播放序号及英雄区辅助链接的文字对比度。
- 无歌曲时长时不再在移动播放器边缘显示无意义的禁用滑块圆点；保持单一音乐音轨，不新增点击 SFX 或第二条 BGM。
- 补充模态语义、焦点和小号控件可读性回归测试；通过五档响应式视口、连续切歌、队列、歌单及 axe 可访问性复查。

### 0.4.13 · 2026-07-31

- 修复连续切歌时旧音频暂停事件与新媒体加载竞争，导致后续歌曲偶发停在缓冲状态、没有启动新音源的问题。
- 自动播放请求现在与歌曲和音源身份精确绑定；初始渲染、错误失效及普通元数据更新仍不会主动加载第三方音频。
- 补充自动播放身份回归测试，并以真实 Chromium 逐首验证 8 条演示音频的加载调用、资源状态及桌面/移动页面控制台。

### 0.4.12 · 2026-07-30

- 本地音乐现在支持后续单独导入同名 LRC，并同步更新当前歌曲、队列、收藏与歌单中的歌词能力。
- LRC 在解码前限制为 500 KB；音频先完成格式过滤再计算 100 首导入上限，异常 MIME 不再把歌词文件误加入播放队列。
- 仅歌曲身份或音源变化时才重新加载音频，补充歌词等元数据不会打断正在播放的歌曲；并阻止旧队列快照覆盖最新歌词状态。
- 补齐本地文件筛选、有界歌词读取、后补歌词和媒体身份回归测试；继续保持单一音乐音轨，不添加干扰歌曲的 SFX/BGM。

### 0.4.11 · 2026-07-30

- 修复 320px 窄屏播放器封面挤压歌曲信息的问题；触屏歌曲列表始终显示播放图标，并将主要播放入口扩大到 40px。
- 解析音乐地址或 ID 时会立即取消旧的关键词搜索；歌单删除确认会在 5 秒后失效，切换页面也会清除，减少过期请求和误删风险。
- 无有效音源时同步清空网页与系统媒体面板的旧进度，失效音源不再残留上一首歌曲的时长状态。
- 升级 checkout 与 setup-node Actions 以适配 Node.js 24 运行器；Pages 专用 Actions 保持已验证版本，部署校验覆盖传播竞态、404 与错误 MIME，并可直接提示 Pages Source 设置。
- 补齐移动交互、媒体状态、请求取消、删除确认和 Pages 传播回归测试；继续保持单一音乐音轨，不添加干扰歌曲的 SFX/BGM。

### 0.4.10 · 2026-07-29

- 修复低高度桌面窗口中侧栏后半部分被播放器遮挡且无法到达的问题；侧栏现在可纵向滚动，各区块不再被 Flex 压缩。
- 首屏不再预加载或主动请求第三方音频，只有用户点击播放后才连接音源，减少无效流量与外部资源失败噪音。
- 新增低高度侧栏与音频加载时机回归测试；复查五档响应式视口、单一音轨、播放请求、控制台及 4xx/5xx，继续不添加干扰歌曲的 SFX/BGM。

### 0.4.9 · 2026-07-29

- 修复“一个来源正常返回空结果、另一个来源故障”被误判为全部来源失败的问题；保留来源级错误报告，异常聚合结果仍不进入缓存。
- 演示搜索延迟现在响应取消信号，健康检查被调用方取消后不再伪装成演示状态，减少切换搜索与页面卸载时的过期工作。
- 优化桌面发现页标题排版、移动侧栏对话框语义、搜索触控目标与演示搜索词，补齐焦点恢复和响应式交互体验。
- 复查受污染本地存储、单音轨播放、桌面/移动视口、控制台与关键静态资源；继续保持单一音乐音轨，不添加干扰歌曲的 SFX/BGM。

### 0.4.8 · 2026-07-28

- 统一播放、来源、识别和下载 URL 的前端信任边界，拒绝可执行协议、远程 Blob 地址及带账号密码的 URL；本地导入仍安全支持会话级 Blob 音频。
- 封面字段同时经过前后端校验，只接受安全的内置封面标识或无凭据 HTTP(S) 图片地址，阻止异常 Provider 响应和受污染的本地存储继续传播。
- 切换识别输入、平台或歌词窗口时会立即取消旧请求；页面卸载也会中止健康、解析、详情与歌词请求，减少无效上游流量和旧响应竞态。
- 补齐 URL、封面与请求取消回归测试，并通过生产构建、Fixture 搜索/歌词链路及关键静态资源无 404 冒烟检查。

### 0.4.7 · 2026-07-28

- 修复旧音频结束事件在新歌曲解析期间错误重置播放状态的问题，切歌反馈不再闪回停止状态。
- 强化弹窗、队列和移动导航的焦点闭环；动态视口下弹窗与页面高度跟随浏览器工具栏变化。
- 触屏设备始终显示卡片播放入口；已播放超过三秒时，即使媒体时长尚未就绪，“上一首”也会先回到本曲开头。
- 新增 Pages 线上冒烟检查，拒绝源码 TSX 入口并验证 favicon、JavaScript、CSS 的状态码与 MIME；补齐播放竞争、焦点陷阱和移动视口回归测试。
- 保持单一音乐音轨，不添加干扰歌曲的点击音效或第二条背景音乐。

### 0.4.6 · 2026-07-27

- 移除 Google Fonts 首屏网络依赖，改用跨平台中英文系统字体栈，避免字体服务失败和布局跳动。
- 新增首屏外部样式依赖回归测试，Pages 构建仅加载同源 HTML、CSS、JavaScript 与站点图标。

### 0.4.5 · 2026-07-27

- 修复 GitHub Pages 直接发布源码导致 `/src/main.tsx` 与 `/favicon.svg` 404 的问题，改为构建并发布 `dist`。
- 生产资源使用 `/LIstener/` 子路径；Pages 启用静态演示模式，不再请求不存在的 Node API。
- 新增 Pages 基址、构建产物和部署模式回归测试，本地开发与真实后端模式保持不变。

### 0.4.4 · 2026-07-27

- 修复缺失站点图标和未识别音乐输入产生的浏览器 404，并加入静态资源回归测试。
- 上一首在播放超过三秒时先回到本曲开头；远程封面失败会显示本地占位，移动歌单横滑增加停靠反馈。
- 保持单一音乐音轨，不添加会干扰歌曲播放的点击音效或第二条常驻背景音乐。

### 0.4.3 · 2026-07-27

- 新增受控 Provider HTTP Client、分页/指定来源 API、离线 Fixture、LRC 时间轴解析、负缓存、被动健康状态、结构化脱敏日志与完整安全/许可证文档。
- 禁用未经确认的网易播放地址构造，保留 Apple 不可试听的合法元数据，并修复跨来源加载时无法取消和超长搜索词静默失败。
- 改善移动端安全区、弹窗焦点顺序、状态播报和小字号对比度；继续保持单一音乐音轨，不添加会与歌曲冲突的点击音效或第二条常驻背景音乐。

### 0.4.2 · 2026-07-26

- 修复音源已连接但仍在等待可播放数据时误显示“正在播放”的问题，新增可取消的缓冲状态，并避免试听元数据到达前显示完整歌曲时长。
- 系统媒体控制新增快退、快进和指定进度，并同步当前歌曲封面、播放状态与安全钳制后的进度信息。
- 增强当前歌曲标识、播放器动效、按钮悬停/按压反馈和正文可读性，同时保持桌面与移动端布局稳定。
- 延长关键播放错误提示，并为缓冲状态与系统进度钳制补齐回归测试。
- 聚合服务不再缓存来源故障期间的残缺搜索结果；客户端断开会取消详情、歌词和下载请求，并统一拦截非法 URL 与歌曲数据。
- 继续保持单一音乐音轨，不添加会与歌曲冲突的点击音效或第二条常驻背景音乐。

### 0.4.1 · 2026-07-26

- 修复音源解析取消只清理界面但未中止前后端请求的问题，并补齐客户端断开与 Provider 取消测试。
- 修复旧歌曲结束事件覆盖新选择、自动下一首失败仍显示播放中、切歌播放失败无提示及过期媒体事件污染状态的问题。
- 搜索上游异常时继续提供演示结果，但会明确显示聚合服务故障；健康检查完成前改为显示正在连接。
- 改善移动端循环控制、播放器触控尺寸、不可播放来源说明、文字对比度与重复队列入口。
- 修复移动菜单语义、歌单弹层初始焦点、删除歌单后的焦点落点、歌词整篇动态播报及来源筛选组语义。
- 保持单一音乐音轨，不新增会干扰歌曲播放的点击音效或常驻背景音乐。

### 0.4.0 · 2026-07-25

- 新增可选 MusicBrainz Provider，提供 CC0 核心录音元数据搜索与 MBID 查询，并遵守应用标识和每秒一次请求限制。
- 新增可选 Audius Provider，通过官方 API 搜索并解析公开、可串流且未设置门槛的音频；播放前会重新检查访问条件。
- API Key 只用于服务端到 Audius 的 HTTPS 请求，禁止重定向和凭据 URL，并拦截返回流地址中的明文或编码 Key。
- 新增可取消的共享请求调度器，按来源限制请求启动频率；异常等待或取消不再阻塞后续合法请求。
- 修复移动端隐藏来源和上一首/下一首的问题，增强权限错误提示、功能文本对比度、触控目标与弹层滚动体验。
- 补齐 MusicBrainz、Audius 地址/ID 识别、前端来源标签、配置说明、许可边界和 Provider 实现参考。

### 0.3.0 · 2026-07-24

Listener 新增合法的多平台识别与完整音乐库交互：

- 支持识别网易、QQ、酷狗、酷我、千千、一听、咪咕、荔枝、蜻蜓、喜马拉雅、5sing、全民 K 歌和 Apple Music 地址或 ID，全程仅做本地格式解析。
- 多来源搜索改为保留各平台结果，并为慢源增加独立超时，单个来源故障不再拖住整体响应。
- 新增歌曲详情、歌词和下载能力接口；下载必须由 Provider 明确授权，未授权请求由服务端返回 403。
- Track 数据新增来源页面、试听/完整播放、歌词、下载与真实音质能力标记，不再笼统宣称所有歌曲都是 CD 音质。
- 新增自建歌单、加入/移除歌曲、播放全部、删除歌单、列表/单曲循环和播放队列清理。
- 新增本地音频与同名 LRC 导入，可在当前会话播放、显示歌词、加入歌单和重新下载；文件对象与 Blob URL 不持久化。
- 搜索页新增平台 ID 解析器、来源跳转与授权状态反馈；播放器新增歌词、下载和加入歌单入口。
- 演示曲库改用正确标明来源的 SoundHelix 演示音频，移除热门歌曲名称与无关音频的错误对应。
- 修复搜索结果串台、播放解析竞态、队列清空后仍出声、失效音源重复失败及平台地址无法回读等问题。
- 改善平板与移动端布局、键盘焦点、动态播放标签、触控目标、滑杆可见度、静音和不可用能力反馈。

### 0.2.0 · 2026-07-23

Listener 从纯前端演示升级为可工作的音乐聚合播放器：

- 新增 Node.js 聚合服务、标准 Provider、搜索缓存、并发合并、去重、限流和健康接口。
- 默认接入 Apple Music 公开检索与试听源，支持真实歌曲信息、封面和试听播放。
- 保留网易云 Provider；因部分区域返回加密结果，改为通过 `ENABLE_NETEASE=true` 显式启用。
- 搜索、收藏、当前歌曲和队列支持远程歌曲，并使用 `source:id` 避免跨来源冲突。
- 修复未解析歌曲切换、空音源播放、快速点歌竞态、首次静音和搜索请求堆积。
- 随机播放现在可用；移除私人电台、虚假歌单、更多操作等无行为入口。
- 改善移动菜单、队列滚动、键盘操作、焦点、滑杆标签、触控面积、文字对比度和加载反馈。

### 0.1.0 · 2026-07-22

- 发布 React + Vite 单页播放器原型。
- 提供发现、演示搜索、收藏、队列和基础播放控制。
- 使用 SoundHelix 演示音频和 CSS 生成封面，尚未包含真实聚合服务。
